// Iterate over an array
// Create an array with multiple elements.
// Use a loop (such as for or forEach()) to iterate over the array.
// Perform an action on each element, such as logging it to the console.

const namesOfBoys = ['Ipheco', 'Prosper', 'Martins', 'Winners', 'Arinze'] // Created an array with multiple elements.

// Use a loop (such as for or forEach()) to iterate over the array.
namesOfBoys.forEach((listBoysName) => {
  console.log(listBoysName) // Perform an action on each element, such as logging it to the console.
})
